# Coding Mentor Agent — 后端接口文档

## 基础信息

- **Base URL**: `http://127.0.0.1:8080`
- **Content-Type**: `application/json`（除 SSE 接口外）
- **字符编码**: UTF-8
- **SSE 接口**: `text/event-stream`

---

## 1. 前端页面

### GET /

返回前端 SPA 页面。

**响应**: `text/html`

**状态码**: 200

---

## 2. 会话管理

### POST /api/sessions

创建新学习会话或恢复已有活跃会话。

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| resume | boolean | 否 | 为 `true` 时恢复最近活跃会话 |

**响应** (200):

```json
{
  "session_id": "sess_xxxxxxxxxxxx",
  "stream_url": "/api/sessions/{session_id}/events"
}
```

**状态码**: 200

---

### GET /api/sessions/{session_id}/snapshot

获取会话完整快照，包含所有对话轮次。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| session_id | string | 会话 ID |

**响应** (200):

```json
{
  "session_id": "sess_xxx",
  "last_event_id": "evt_xxx",
  "turns": [
    {
      "turn_id": "turn_xxx",
      "status": "done",
      "user_message": {
        "text": "用户消息内容",
        "code_ref": "code_turn_xxx"
      },
      "assistant_messages": [
        {
          "message_id": "msg_xxx",
          "text": "AI 回复内容"
        }
      ]
    }
  ],
  "active_exercise": null,
  "active_practice_contract": null
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| session_id | string | 会话 ID |
| last_event_id | string\|null | 最新 SSE 事件 ID，用于断线重连 |
| turns | array | 对话轮次列表 |
| turns[].turn_id | string | 轮次 ID |
| turns[].status | string | 轮次状态："streaming" / "done" |
| turns[].user_message.text | string | 用户消息文本 |
| turns[].user_message.code_ref | string\|null | 关联的代码引用 ID |
| turns[].assistant_messages | array | AI 回复列表 |
| active_exercise | null | 当前活跃练习 |
| active_practice_contract | null | 当前活跃练习合约 |

**状态码**: 200 / 404（会话不存在）

---

### DELETE /api/sessions/{session_id}

物理删除指定会话及其所有关联数据（消息、诊断、练习、学习事件等）。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| session_id | string | 会话 ID |

**响应** (200):

```json
{
  "message": "Session sess_xxx deleted",
  "ok": true
}
```

**状态码**: 200 / 404（会话不存在）

---

## 3. 消息对话

### POST /api/sessions/{session_id}/messages

发送消息并获取 AI 导师回复。同时触发 SSE 事件推送。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| session_id | string | 会话 ID |

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| message | string | 是* | 用户消息文本（最大 4000 字符） |
| code | string | 否 | 关联的代码内容 |
| practice_submission | object | 否 | 练习提交（见下方） |

> *`message` 和 `practice_submission` 至少提供一个

**practice_submission 对象**:

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| kind | string | 是 | 固定值 `"practice_submission"` |
| practice_contract_id | string | 是 | 练习合约 ID |
| code | string | 是 | 提交的代码 |

**响应** (200):

```json
{
  "accepted": true,
  "turn_id": "turn_xxx",
  "assistant_message": "AI 导师的回复内容..."
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| accepted | boolean | 是否接受 |
| turn_id | string | 对话轮次 ID |
| assistant_message | string | AI 回复全文（Markdown 格式） |

**状态码**:
- 200 — 成功
- 400 — 消息为空或超过 4000 字符
- 404 — 会话不存在
- 500 — 服务器内部错误

---

### GET /api/sessions/{session_id}/events

SSE 实时事件流，用于接收 AI 回复推送。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| session_id | string | 会话 ID |

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| after_seq | integer | 否 | 从指定序号之后开始推送（断线重连用） |

**响应**: `text/event-stream`

**事件类型**:

**`message_delta`** — AI 回复内容：

```json
{
  "type": "message_delta",
  "turn_id": "turn_xxx",
  "message_id": "msg_xxx",
  "delta": "AI 回复的完整文本..."
}
```

**`done`** — 当前轮次完成：

```json
{
  "type": "done",
  "turn_id": "turn_xxx"
}
```

**SSE 格式**:
```
id: evt_xxx
data: {"type":"message_delta","turn_id":"turn_xxx",...}

```

**状态码**: 200 / 404

---

## 4. 诊断测评

### GET /api/diagnostics/next

获取下一道诊断测评题目。题目按知识点轮询出题。

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| session_id | string | 否 | 会话 ID，为空则使用最近活跃会话 |

**响应** (200 — 有题目):

```json
{
  "diagnostic_id": "diag_xxx",
  "question_index": 0,
  "total_answered": 0,
  "concept_id": "datatypes",
  "concept_name": "基本数据类型",
  "question": "在 Python 中，`type(42)` 的输出是什么？",
  "options": ["<class 'int'>", "<class 'float'>", "<class 'str'>", "<class 'list'>"],
  "difficulty": 1,
  "completed": false
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| diagnostic_id | string | 诊断会话 ID（提交答案时需要） |
| question_index | integer | 当前题目序号（从 0 开始） |
| total_answered | integer | 已答题数 |
| concept_id | string | 知识点 ID |
| concept_name | string | 知识点名称 |
| question | string | 题目文本 |
| options | string[] | 选项列表 |
| difficulty | integer | 难度等级 |
| completed | boolean | 是否已完成全部测评 |

**响应** (200 — 已完成):

```json
{
  "diagnostic_id": "diag_xxx",
  "completed": true,
  "total_answered": 6,
  "message": "诊断测评已完成！"
}
```

**状态码**: 200 / 400（无活跃会话）

---

### POST /api/diagnostics/{diagnostic_id}/answers

提交诊断测评答案，自动判题并更新掌握度。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| diagnostic_id | string | 诊断会话 ID |

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| answer_index | integer | 是 | 选择的选项索引（0-based） |
| concept_id | string | 是 | 知识点 ID |
| question | string | 是 | 题目文本 |
| difficulty | integer | 是 | 难度等级 |

**响应** (200):

```json
{
  "correct": true,
  "message": "正确！"
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| correct | boolean | 是否回答正确 |
| message | string | 反馈信息 |

**状态码**: 200 / 500

**知识点列表**:

| concept_id | 名称 |
|---|---|
| datatypes | 基本数据类型 |
| functions | 函数 |
| loops | 循环 |
| strings | 字符串 |
| lists | 列表 |
| file_handling | 文件处理 |

---

## 5. 编程练习

### POST /api/sessions/{session_id}/practice

请求生成编程练习。需先完成诊断测评。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| session_id | string | 会话 ID |

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| concept_ids | string[] | 否 | 指定练习的知识点 ID 列表，为空则自动选择最薄弱的知识点 |

**响应** (200 — 测评未完成):

```json
{
  "kind": "practice_locked",
  "message": "请先完成诊断测评后再请求练习。",
  "reason": "locked_by_diagnostic"
}
```

**响应** (200 — 练习就绪):

```json
{
  "kind": "exercise_ready",
  "message": "已创建 基本数据类型 的练习，请在代码编辑器中提交你的代码。",
  "next_step": "在编辑器中编写代码，然后点击提交",
  "exercise": {
    "id": "pc_xxx",
    "practice_contract_id": "pc_xxx",
    "title": "基本数据类型 练习",
    "difficulty": 1,
    "concept_ids": ["datatypes"],
    "prompt_md": "请编写一个与 基本数据类型 相关的 Python 程序...",
    "acceptance_checklist": ["代码可以运行", "输出符合预期", "使用了目标概念"],
    "submission": {
      "endpoint": "/api/sessions/{session_id}/messages",
      "enabled": true
    }
  }
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| kind | string | `"practice_locked"` 或 `"exercise_ready"` |
| message | string | 提示信息 |
| reason | string | 锁定原因（仅 locked 时） |
| next_step | string | 下一步指引（仅 ready 时） |
| exercise.id | string | 练习合约 ID |
| exercise.title | string | 练习标题 |
| exercise.difficulty | integer | 难度 |
| exercise.concept_ids | string[] | 关联知识点 |
| exercise.prompt_md | string | 练习题目（Markdown） |
| exercise.acceptance_checklist | string[] | 验收标准 |
| exercise.submission.endpoint | string | 提交代码的接口地址 |
| exercise.submission.enabled | boolean | 是否允许提交 |

**状态码**: 200

---

### POST /api/exercises/{exercise_id}/submissions

提交练习代码并获取批改结果。

**路径参数**:

| 参数 | 类型 | 说明 |
|---|---|---|
| exercise_id | string | 练习合约 ID |

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| code | string | 是 | 提交的 Python 代码 |

**响应** (200):

```json
{
  "status": "passed",
  "review": "批改反馈...",
  "details": {}
}
```

**状态码**: 200 / 400（代码为空）

---

## 6. 学习进度

### GET /api/progress/me

获取当前用户的学习进度总览。

**查询参数**:

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| session_id | string | 否 | 会话 ID，为空则使用最近活跃会话 |

**响应** (200):

```json
{
  "profile_summary": "Python 课程学习者。",
  "current_level": "未诊断",
  "current_goal": null,
  "diagnostic_state": "active",
  "practice_state": "locked_by_diagnostic",
  "handoff_state": "not_ready",
  "learning_start": {},
  "course_progress_percent": 0,
  "mastery": [
    {
      "concept_id": "datatypes",
      "name": "基本数据类型",
      "mastery_level": 80,
      "confidence": 0.7,
      "review_priority": 0
    }
  ],
  "weak_concepts": [
    {
      "concept_id": "functions",
      "name": "函数",
      "reason": "掌握度仅 30%"
    }
  ],
  "curriculum": [
    {
      "id": "unit_01",
      "title": "Python 入门",
      "concept_ids": [],
      "mastery_percent": 0,
      "status": "current"
    }
  ],
  "diagnostic_feedback": {
    "performance_summary": "已完成 6 个概念的诊断",
    "mastery_summary": "弱项: 2 个概念",
    "learning_start": "基本数据类型"
  }
}
```

**字段说明**:

| 字段 | 类型 | 说明 |
|---|---|---|
| profile_summary | string | 学习画像摘要 |
| current_level | string | 当前等级 |
| current_goal | string\|null | 学习目标 |
| diagnostic_state | string | 诊断状态：`"not_started"` / `"active"` / `"completed"` |
| practice_state | string | 练习状态：`"locked_by_diagnostic"` / `"available_after_explicit_request"` |
| handoff_state | string | 交接状态 |
| learning_start | object | 学习起点（诊断完成后有值） |
| course_progress_percent | integer | 课程进度百分比 |
| mastery | array | 各概念掌握度列表 |
| mastery[].concept_id | string | 知识点 ID |
| mastery[].name | string | 知识点名称 |
| mastery[].mastery_level | integer | 掌握度（0-100） |
| mastery[].confidence | number | 置信度（0-1） |
| mastery[].review_priority | integer | 复习优先级 |
| weak_concepts | array | 薄弱知识点列表 |
| weak_concepts[].concept_id | string | 知识点 ID |
| weak_concepts[].name | string | 知识点名称 |
| weak_concepts[].reason | string | 薄弱原因 |
| curriculum | array | 课程大纲 |
| curriculum[].id | string | 单元 ID |
| curriculum[].title | string | 单元标题 |
| curriculum[].concept_ids | string[] | 关联知识点 ID |
| curriculum[].mastery_percent | integer | 掌握百分比 |
| curriculum[].status | string | 状态：`"current"` / `"upcoming"` |
| diagnostic_feedback | object\|null | 诊断反馈（诊断完成后有值） |

**状态码**: 200

---

## 7. 代码执行

### POST /api/code/run

在 Docker 沙箱中执行 Python 代码（需 Docker 运行中）。

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| code | string | 是 | 要执行的 Python 代码 |

**响应** (200 — 沙箱可用):

```json
{
  "ok": true,
  "request_id": "api_run",
  "result": {
    "status": "passed",
    "exit_code": 0,
    "stdout": "2\n",
    "stderr": "",
    "traceback": "",
    "duration_ms": 150,
    "truncated": false
  }
}
```

**响应** (200 — 沙箱不可用):

```json
{
  "ok": false,
  "code": "SANDBOX_ERROR",
  "message": "Docker 不可用: ..."
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| ok | boolean | 是否成功执行 |
| request_id | string | 请求 ID |
| result.status | string | 执行状态：`"passed"` / `"failed"` / `"sandbox_error"` |
| result.exit_code | integer | 进程退出码 |
| result.stdout | string | 标准输出 |
| result.stderr | string | 标准错误 |
| result.traceback | string | 异常堆栈 |
| result.duration_ms | integer | 执行耗时（毫秒） |
| result.truncated | boolean | 输出是否被截断 |

**状态码**: 200 / 400（代码为空）/ 503（沙箱不可用）

---

## 8. 数据管理

### GET /api/data/export

导出所有学习数据概览。

**响应** (200):

```json
{
  "sessions": [
    {
      "id": "sess_xxx",
      "status": "active",
      "started_at": "2026-06-12T08:21:01.984374+00:00"
    }
  ],
  "total_turns": 5,
  "total_concepts": 6
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| sessions | array | 所有会话列表 |
| sessions[].id | string | 会话 ID |
| sessions[].status | string | 会话状态 |
| sessions[].started_at | string | 开始时间（ISO 8601） |
| total_turns | integer | 总对话轮次 |
| total_concepts | integer | 知识库概念总数 |

**状态码**: 200

---

### POST /api/data/delete

删除所有本地学习数据（不可恢复）。

**请求体** (JSON):

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| confirm | string | 是 | 必须为 `"DELETE_LOCAL_LEARNING_DATA"` |

**响应** (200):

```json
{
  "message": "学习数据已清除"
}
```

**状态码**: 200 / 400（确认 token 不正确）

---

## 附录：通用约定

### ID 格式

| 前缀 | 示例 | 说明 |
|---|---|---|
| sess_ | sess_9ded49b43efb | 会话 ID |
| turn_ | turn_721590251c8c | 对话轮次 ID |
| msg_ | msg_a1b2c3d4e5f6 | 消息 ID |
| evt_ | evt_a1b2c3d4e5f6 | SSE 事件 ID |
| diag_ | diag_3db5f20e6f90 | 诊断会话 ID |
| pc_ | pc_a1b2c3d4e5f6 | 练习合约 ID |

所有 ID 格式为 `{前缀}_{12位hex}`，由服务端生成。

### 错误响应格式

```json
{
  "detail": "错误描述信息"
}
```

### 接口汇总

| # | 方法 | 路径 | 说明 |
|---|---|---|---|
| 1 | GET | `/` | 前端页面 |
| 2 | POST | `/api/sessions` | 创建/恢复会话 |
| 3 | GET | `/api/sessions/{id}/snapshot` | 会话快照 |
| 4 | POST | `/api/sessions/{id}/messages` | 发送消息 |
| 5 | GET | `/api/sessions/{id}/events` | SSE 事件流 |
| 6 | GET | `/api/diagnostics/next` | 获取诊断题目 |
| 7 | POST | `/api/diagnostics/{id}/answers` | 提交诊断答案 |
| 8 | POST | `/api/sessions/{id}/practice` | 请求练习 |
| 9 | POST | `/api/exercises/{id}/submissions` | 提交练习代码 |
| 10 | GET | `/api/progress/me` | 学习进度 |
| 11 | POST | `/api/code/run` | 沙箱执行代码 |
| 12 | GET | `/api/data/export` | 导出数据 |
| 13 | POST | `/api/data/delete` | 删除数据 |
